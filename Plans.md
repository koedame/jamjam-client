# Plans.md - タスク管理

> このファイルは Claude Code がタスクを管理するために使用します。
> プランモードで計画を立ててタスクを追加し、実装完了時にチェックを更新します。
> 完了した項目は末尾の「完了済みタスク（要約）」に移します。

## 記法

- `[ ]` 未着手 / `[>]` 進行中 / `[x]` 完了 / `[!]` 外部要因でブロック中 / `[-]` キャンセル

作業項目の識別子は要求 ID（`REQ-*`）とする。連番のフェーズ番号は使わない（[ADR-023](./docs-spec/adr/ADR-023-drop-phase-as-identifier.md)）。要求に紐づかない項目は見出しの文言で識別する。

グループは進行順ではなく**何を待っているか**で分ける。半年着手されていない項目が「進行中」に見える状態を避けるためである。

未検証の要求一覧は [docs-spec/traceability.md](./docs-spec/traceability.md) が自動生成する。本ファイルはそこに「なぜ未達か・何が必要か」を補う。

---

## 実環境待ち

実機・実回線・複数マシンが必要なもの。コードを書いても解決しない。

### REQ-CORE-004: 国内光回線での総遅延測定

`zero-latency` プリセットで RTT 20ms の光回線越しに、総片道遅延 12ms 以下を実測する。

- [!] 光回線2拠点（または実回線相当のネットワークエミュレーション）を用意
- [ ] 実測値と ADR-019 の設計値（1.33ms）+ 実 RTT の差分を記録
- **必要なもの**: 実際の光回線2拠点
- **代替検証は完了**: REQ-LAT-115（設計バジェット）と REQ-LAT-027（ループバック実測が設計値と一致）

### REQ-LAT-102: 複数参加者の遅延を個別表示

- [!] 複数マシン
- [ ] メッシュ構成（`Session` の複数ピア対応を音声経路まで配線）
- [ ] 参加者ごとの RTT / ジッター表示と、最も遅延の大きい参加者のハイライト
- [ ] マルチノード E2E ハーネス（プロセスごとの仮想オーディオデバイス）
- **補足**: シグナリング側の参加者通知は REQ-CON-111 で検証済み。残るのは音声経路のメッシュ化と表示

### REQ-LAT-123: ASIO 使用時の低遅延

Windows で ASIO ドライバ選択時にデバイス遅延 3ms 以下になることを検証する。

- [!] Windows 実機 + ASIO 対応オーディオインターフェース
- [ ] Windows 実機で手元実行（CI では回さない）

---

## 手動確認待ち

実装は完了しており、人が動かして確かめる作業だけが残っているもの。

### テストルーム機能の動作確認

- [x] テストルームのコードをアプリに持たせず、サーバーのルーム一覧で示されたときだけ入口を出す
- [ ] `cargo tauri dev` で起動し、テストルームに接続できることを確認

### 招待リンクの動作確認

スキーム登録と受け取りハンドラは実装・検証済み（REQ-CON-103）。OS への登録はインストール済みアプリでのみ有効なため、動作確認はビルド後に行う。

- [ ] `cargo tauri build` 後、`jamjam://join/<コード>` をブラウザやターミナルから開いてアプリが起動・参加することを確認
- [ ] 起動中のアプリに対してリンクを開いた場合も参加すること

### 端末アイデンティティの動作確認（REQ-IDT-001 / REQ-IDT-006）

導出・検証・永続化は自動テストで検証済み（REQ-IDT-001〜007）。起動時の画面と端末IDの非表示は GUI E2E（ADR-025）が自動検証するようになった。

- [x] サインイン画面が出ず接続画面から始まること → `tests/e2e/tests/gui.rs`（REQ-GUI-001）
- [x] 端末識別子が UI に出ないこと → 同上（REQ-GUI-002）
- **補足**: `device_identity.json` の 0600 生成と再起動時の同一性は単体テスト（REQ-IDT-001 / 006）で検証済み。旧 `config.toml` の後方互換も `config.rs` の回帰テストで担保済み

### macOS のマイク許可ダイアログ（REQ-DIST-001）

`NSMicrophoneUsageDescription` の宣言は `src-tauri/Info.plist` にあり、存在は `tests/distribution_config_test.rs` が検証している（ADR-029）。Tauri がそれをバンドルの Info.plist にマージし、macOS がそれを読んでプロセスを終了させないことは、macOS で作ったバンドルでしか確かめられない。

- [ ] macOS で `cargo tauri build` し、`jamjam.app/Contents/Info.plist` に `NSMicrophoneUsageDescription` があること
- [ ] そのアプリを初回起動してセッションに入ると、説明文付きのマイク許可ダイアログが出て、許可後に音声が送れること（ターミナルから `cargo tauri dev` で起動すると権限がターミナルのものになるため、Finder から起動する）

### GUI E2E の残りギャップ（ADR-025 / ADR-026）

24 シナリオまで自動化済み（REQ-GUI-001〜015）。実音声も検証範囲に入った — ループバックデバイスに流したトーンがレベルメーターに現れ（REQ-GUI-013）、GUI 2 台の間を流れて受信側のメーターが反応する（REQ-GUI-014）。これで [ADR-026](./docs-spec/adr/ADR-026-gui-audio-path.md) の 3 つの欠陥が露出し、修正済みである。

- [ ] 音量・パンの操作が実際の音声波形に反映されること（歪み・SNR の測定が必要）
- **必要なもの（実音声シナリオ全体）**: ループバックオーディオドライバ。macOS は `brew install blackhole-2ch`（**sudo が必要でシステムのオーディオスタックを変更する**）、Windows は VB-Cable、Linux は PipeWire null-sink。CI では実行できないため、開発者のローカルでのみ走る
- **注意**: このドライバは書き込みを加算し、書き込みを止めても内容を返し続ける。無音を前提にしたシナリオは書けない（測定値は `tests/e2e/src/pom/loopback_audio.rs`）

---

## 判断待ち

製品要件としての可否が未決のもの。実装するかどうかを先に決める必要がある。

### 狭帯域回線への対応

ADR-022 で**対象外**と決定済み。再検討する場合はここから始める。

- [ ] 対象回線を光回線以外に広げるかを判断
- [ ] 広げる場合、フレームサイズ戦略の ADR を新規作成（Opus は 48kHz で 120/240/480/960/1920/2880 サンプルのみ受け付け、現行プリセットの 32/64/128/256 はいずれも非該当）
- **トレードオフ**: Opus 採用にはフレームサイズ変更か再パケット化が必要で、どちらも遅延が増える。「帯域効率より遅延削減を優先する」（AGENTS.md）と逆行する

### ADR-012 段階4（チャット機能）の記述矛盾

ADR-012 の段階4 は「チャット機能（将来）／方式: WebRTC DataChannel」と記載しているが、実態と 2 点で食い違う。

| 記述 | 実態 |
|------|------|
| 「将来」 | 実装済み（`src/main.rs`, `src/network/signaling.rs`, `src-tauri/src/signaling.rs`, UI の ChatPanel） |
| 「WebRTC DataChannel」 | シグナリングサーバー経由のブロードキャスト（`SignalingMessage::ChatMessage`）。ADR-002 は WebRTC を明確に不採用としている |

権威ある仕様は `docs-spec/api/signaling.md` §ChatMessage であり、そちらは実装と一致している。ADR-012 の段階4 は、コード署名とは無関係なロードマップの下書きが残ったものと見られる。

- [ ] チャットのトランスポート決定を ADR として起票するか、ADR-012 段階4 の記述を撤回するかを判断
- **判断が必要な理由**: ADR は追加のみのため、既存記述の撤回にも新規 ADR が要る。また当時の設計意図を後から代弁するのは避けたい

### カスタムプリセットの保存

ADR-022 で**対象外**と決定済み。

- [ ] 任意設定の保存を認めるかを判断
- **トレードオフ**: ADR-019 / ADR-021 が各プリセットに検証済みの遅延バジェットと FEC 構成を与えている。任意の組み合わせを保存できると、検証されていない遅延特性の設定が生成される

---

## 参照

- [AGENTS.md](./AGENTS.md) - AIエージェント向け開発ガイド（開発フロー）
- [docs-spec/](./docs-spec/) - 仕様書
- [docs-spec/traceability.md](./docs-spec/traceability.md) - 要求と検証の対応表（自動生成）

---

## 完了済みタスク（要約）

> ADR-023 より前の項目は「Phase N」という連番で呼ばれていた。

- ✅ Storybook UI と Tauri GUI の統一（ChatPanel, SettingsPanel）
- ✅ Settings Window - Storybook コンポーネント移植
- ✅ 診断結果ダッシュボード UI
- ✅ テストルーム接続ボタン追加（Storybook）
- ✅ UI コンポーネント設計ルール追加（`.claude/rules/ui-component-rules.md`）
- ✅ UI デザイン刷新の全面反映（`ui/`・仕様書） + ホスト概念廃止（ADR-016） + メール OTP アカウントシステム導入
- ✅ 反復V字モデル（W字）＋要求トレーサビリティ導入（ADR-018） + プリセット遅延バジェット確定（ADR-019）。要求 70 件を ID 化、47 件を検証、形骸化テストと捏造 E2E 測定値を撤廃、UI テストを CI に追加
- ✅ V字右辺のギャップ解消（ADR-020〜022）。JitterBuffer / FEC / PLC を受信経路に配線、接続品質判定と帯域測定を実装、自動再接続を実装、狭帯域回線とカスタムプリセットを対象外と決定。要求 83 件・`must` 79 件・検証済み 80 件
- ✅ 「Phase」を識別子として使わないことを決定（ADR-023）。Plans.md を要求 ID 主キーに再構成、文書内の段階表を改名
- ✅ 依存ライブラリの脆弱性を解消（Storybook 8→10 移行、未使用パッケージ削除）。`npm audit` が Rust/ui すべてクリーン
- ✅ 招待URLのOSスキーム登録（`tauri-plugin-deep-link`）。起動時・起動中の両方でリンクを受け取り、コードを解析して参加する
- ✅ Pencil.dev 対応。`ui.pen`（Atoms/Molecules/Organisms、48コンポーネント）を Git 管理し、MCP 連携とワークフローを `ui-component-rules.md` に文書化
- ✅ 会員登録を廃止し端末アイデンティティに移行（ADR-024）。Ed25519 鍵ペアをローカル生成し公開鍵から26文字の識別子を導出、ハンドシェイクで署名検証。メールOTP・`reqwest` を撤去。要求 95 件・`must` 90 件・検証済み 92 件
- ✅ 受信音声を単段バッファにした（ADR-028）。出力コールバックが `PlayoutBuffer` から直接引くようにし、ジッタバッファ→再生FIFO の 2 段構成を解消。設定した段数が実行時に存在しない問題と、再生FIFO の遅延が遅延表示に入っていない問題を修正。250µs のポーリングを廃止。フレームは到着した長さのまま再生する（リサンプル後の長さを固定に丸めるとピッチが変わるため）
- ✅ CLI に GUI のコア操作を追加（ADR-027）。`create-room`（招待コード発行）・セッション中の `/mute` `/stats`・`preset list|use`・`devices set` を追加し、設定ファイルを GUI と共有（`AppConfig` をコアライブラリへ移動）。CLI はデバッグ用途と位置づけ、E2E 自動化は GUI E2E が担うと決定。`architecture.md` §8.2 の「全機能をCLIから利用可能」という記述が実態と一致していなかったのを解消
- ✅ GUI 同士の音声経路を成立させた（ADR-026）。アドレス交換の欠落・受信ループがネットワークタスクを飢餓させる問題・ジッタバッファが未到着を損失と扱う問題の 3 つを修正し、参加応答に招待コードを含めた（参加者が別のルームコードを表示していた）。実音声 GUI E2E で検証（REQ-GUI-012〜015、REQ-CON-024〜026、REQ-AUD-030、REQ-LAT-029）。要求 116 件・`must` 111 件・検証済み 113 件
- ✅ GUI E2E 制御チャネルとページオブジェクトモデルを導入（ADR-025）。実アプリを起動して DOM を読み・操作する層を追加し、V字右辺の GUI の穴を埋めた。シグナリングサーバー + 2 アプリ構成で参加者一覧・ミキサー・チャットまで自動化。19 シナリオ（REQ-GUI-001〜011）。レベルメーターの RMS 計算を1関数に統合し検証（REQ-AUD-029、3箇所の重複を解消）。要求 107 件・`must` 102 件・検証済み 104 件
- ✅ 配布前にアプリ設定を固めた（ADR-029）。macOS のマイク使用の説明（`Info.plist`）を宣言し、アプリ識別子を `me.koeda.jamjam` に変更し、CSP を有効化して `window.__TAURI__` の公開をやめた。CSP がインラインのスクリプトを止めることと、GUI E2E が変更前と同じ結果になることを Linux で確認。設定は `tests/distribution_config_test.rs` で固定（REQ-DIST-001〜004）
- ✅ 再生中の段数変更と自動調整を実際の遅延に効かせた（ADR-031）。段数を増やすときは無音を挟み、減らすときはフレームを捨てる。プリセット切替が接続時の範囲で切られていた問題を直し、`adapt()` を受信経路から 1 秒ごとに呼んで REQ-LAT-108 の通知を実装（判定は直近の区間の補間率、縮めるのは 10 区間続いてから、パススルーは固定）
