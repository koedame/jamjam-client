{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Plugin commands the webview calls: the launch URL and the invite-link listener (ui/src/lib/deepLink.ts). Nothing else is granted.",
  "windows": ["main", "settings", "chat", "help-*"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "deep-link:allow-get-current"
  ]
}
